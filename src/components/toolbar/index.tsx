import React, { FunctionComponent } from 'react';

type ToolbarProps = {

}

const Toolbar: FunctionComponent<ToolbarProps> = ({ }: ToolbarProps) => {


    return (
        <div className='w-full bg-slate-600'>

        </div>
    )
}

export default Toolbar